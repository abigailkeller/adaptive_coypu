#  - Here step = 20, so we have 101 states from 0 to 2000.
#  - Trade-off: finer step => more precise but slower.
seq_N <- seq(0, K, by = 20)
S <- length(seq_N)  # number of states
# Discrete action space for control effort u in [0,1]:
#  - Here step = 0.05, so we have 21 effort levels.
seq_u <- seq(0, 1, by = 0.05)
A <- length(seq_u)  # number of actions
cat("Number of states S =", S, "\n")
cat("Number of actions A =", A, "\n")
# -----------------------------#
# 2) Parameters (toy / to calibrate)
# -----------------------------#
# Intrinsic growth rate (logistic dynamics): controls growth at low N
r <- 0.9
# Controls how quickly the removal fraction increases with effort u
# Larger alpha => smaller u already removes a lot (stronger response)
alpha <- 1.5
# Tolerable threshold (soft constraint): above this, penalties apply
N_tol <- 400
# -----------------------------#
# 3) Helper functions
# -----------------------------#
# Clamp a number into [lo, hi] to avoid leaving state support
clamp <- function(x, lo, hi) pmin(pmax(x, lo), hi)
# Project a continuous value onto a discrete grid by nearest neighbor
# (this is the "state discretization" step).
round_to_grid <- function(x, grid) {
grid[which.min((grid - x)^2)]
}
# Removal fraction as a saturating function of effort u:
#   q(u) = 1 - exp(-alpha*u)
# Properties:
#   q(0)=0, increasing, saturating (diminishing returns).
q_u <- function(u) 1 - exp(-alpha * u)
# -----------------------------#
# 4) Population dynamics (1-step)
# -----------------------------#
# Deterministic update:
#   N_{t+1} = N_t + r N_t (1 - N_t/K) - q(u) N_t
#
# Interpretation:
# - logistic growth adds recruits at low N and slows near K
# - control removes a fraction q(u) of the population
step_fun <- function(N, u, m) {
Nnext <- N + r * N * (1 - (N / K) ^ m) - q_u(u) * N
clamp(Nnext, 0, K)
}
# -----------------------------#
# 5) Reward function = negative total costs
# -----------------------------#
# Components:
# - damage(N): increasing convex function of N (impacts accelerate with abundance)
# - cost(u): increasing convex function of u (marginal effort becomes harder/costlier)
# - penalty(N): soft constraint if N > N_tol (risk aversion / unacceptable state)
reward_fun <- function(N, u) {
# Damages (toy): linear + quadratic
# - linear = baseline nuisance
# - quadratic = rapidly increasing impacts at high N
damage <- 0.1 * N + 0.001 * N^2
# Management cost (toy): convex in u
# - proportional part + quadratic part for increasing marginal costs
# cost <- 50 * u + 200 * u^2
cost <- 500 * u + 1000 * u^2
# cost <- 750 * u + 2000 * u^2
# Penalty above threshold (toy): increases linearly beyond N_tol
penalty <- if (N > N_tol) 2000 * (N - N_tol) / N_tol else 0
# Reward = negative costs (maximize reward <=> minimize cost)
-(damage + cost + penalty)
}
# -----------------------------#
# 6) Build transition kernel P and reward matrix R
# -----------------------------#
# MDPtoolbox expects:
# - P: [S, S, A] transition probability array
#      P[s, s_next, a] = Prob(next_state = s_next | current_state = s, action = a)
# - R: [S, A] reward matrix
#      R[s, a] = immediate reward when choosing action a in state s
#
# Here transitions are deterministic on the grid:
# for each (s,a), we compute N_{t+1} (continuous), snap it to the nearest grid
# state, and assign probability 1 to that next state.
# get deterministic P and R
get_P_R <- function(m) {
P <- array(0, dim = c(S, S, A))
R <- matrix(0, nrow = S, ncol = A)
for (s in seq_len(S)) {
# Current abundance for state index s
N <- seq_N[s]
for (a in seq_len(A)) {
# Current effort for action index a
u <- seq_u[a]
# Continuous next abundance under dynamics + control
Nnext <- step_fun(N, u, m)
# Discretize to the nearest grid state
Ndisc <- round_to_grid(Nnext, seq_N)
# Convert discretized abundance value to a state index
s_next <- which(seq_N == Ndisc)
# Deterministic transition: prob 1 to s_next
P[s, s_next, a] <- 1
# Immediate reward at (N,u)
R[s, a] <- reward_fun(N, u)
}
}
return(list(P = P, R = R))
}
out_1 <- get_P_R(1)
out_1.25 <- get_P_R(1.25)
out_0.4 <- get_P_R(0.4)
# # get uncertain P and R
# P_prob <- array(0, dim = c(S, S, A))
# R_prob <- matrix(0, nrow = S, ncol = A)
# m_set <- c(1, 1.2, 0.4)
#
# for (s in seq_len(S)) {
#
#   # Current abundance for state index s
#   N <- seq_N[s]
#
#   for (a in seq_len(A)) {
#
#     # Current effort for action index a
#     u <- seq_u[a]
#
#     s_next <- c()
#     for (m in seq_len(length(m_set))) {
#
#       # Continuous next abundance under dynamics + control
#       Nnext <- step_fun(N, u, m_set[m])
#
#       # Discretize to the nearest grid state
#       Ndisc <- round_to_grid(Nnext, seq_N)
#
#       # Convert discretized abundance value to a state index
#       s_next[m] <- which(seq_N == Ndisc)
#     }
#
#     # Probabilistic transition: prob 1 to s_next
#     P_prob[s, as.integer(names(table(s_next))), a] <- (
#       as.vector(table(s_next) / length(s_next))
#     )
#
#     # Immediate reward at (N,u)
#     R_prob[s, a] <- reward_fun(N, u)
#   }
# }
# Sanity check: dimensions + row sums (each row of P[,,a] sums to 1)
mdp_check(out_1$P, out_1$R)
# -----------------------------#
# 7) Solve infinite-horizon discounted MDP (value iteration)
# -----------------------------#
# discount must satisfy 0 <= gamma < 1
# epsilon controls convergence tolerance
sol_1 <- mdp_value_iteration(out_1$P, out_1$R, discount = 0.99, epsilon = 1e-6)
sol_1.25 <- mdp_value_iteration(out_1.25$P, out_1.25$R,
discount = 0.99, epsilon = 1e-6)
sol_0.4 <- mdp_value_iteration(out_0.4$P, out_0.4$R,
discount = 0.99, epsilon = 1e-6)
# sol_prob <- mdp_value_iteration(P_prob, R_prob, discount = 0.99, epsilon = 1e-6)
# -----------------------------#
# 8) Extract optimal policy u*(N) and plot
# -----------------------------#
policies <- data.frame(
N = c(seq_N, seq_N, seq_N),
policy = c(seq_u[sol_1$policy], seq_u[sol_1.25$policy],
seq_u[sol_0.4$policy]),
m = c(rep(1, length(seq_N)), rep(1.25, length(seq_N)),
rep(0.4, length(seq_N)))
)
# -----------------------------#
# 9) Simulate a trajectory under the optimal and sub-optimal policies
# -----------------------------#
# Because transitions are deterministic on the grid, the trajectory is
# deterministic given N0 (after snapping to the nearest grid point).
simulate_policy <- function(N0, n_years = 30, sol, m) {
N_path <- numeric(n_years)
u_path <- numeric(n_years)
reward_path <- numeric(n_years)
# Initial abundance (clamped to [0,K])
N_path[1] <- clamp(N0, 0, K)
for (t in 1:(n_years - 1)) {
# Find the closest state index (in case N0 is not exactly on the grid)
s <- which.min((seq_N - N_path[t])^2)
# Optimal action index and corresponding effort
a <- sol$policy[s]
u <- seq_u[a]
# Store action and reward at current time
u_path[t] <- u
reward_path[t] <- reward_fun(N_path[t], u)
# Step forward under dynamics and snap to grid (consistent with the MDP)
Nnext <- step_fun(N_path[t], u, m)
N_path[t + 1] <- round_to_grid(Nnext, seq_N)
}
# Store action and reward at final time step
sfinal <- which.min((seq_N - N_path[n_years])^2)
u_path[n_years] <- seq_u[sol$policy[sfinal]]
reward_path[n_years] <- reward_fun(N_path[n_years], u_path[n_years])
data.frame(
year = 1:n_years,
N = N_path,
u = u_path,
reward = reward_path
)
}
traj_m1 <- list(
#sol_prob = simulate_policy(N0 = 800, n_years = 40, sol = sol_prob, m = 1),
sol_1 = simulate_policy(N0 = 800, n_years = 40, sol = sol_1, m = 1),
sol_0.4 = simulate_policy(N0 = 800, n_years = 40, sol = sol_0.4, m = 1),
sol_1.25 = simulate_policy(N0 = 800, n_years = 40, sol = sol_1.25, m = 1)
)
traj_m0.4 <- list(
#sol_prob = simulate_policy(N0 = 800, n_years = 40, sol = sol_prob, m = 0.4),
sol_1 = simulate_policy(N0 = 800, n_years = 40, sol = sol_1, m = 0.4),
sol_0.4 = simulate_policy(N0 = 800, n_years = 40, sol = sol_0.4, m = 0.4),
sol_1.25 = simulate_policy(N0 = 800, n_years = 40, sol = sol_1.25, m = 0.4)
)
traj_m1.25 <- list(
#sol_prob = simulate_policy(N0 = 800, n_years = 40, sol = sol_prob, m = 1.2),
sol_1 = simulate_policy(N0 = 800, n_years = 40, sol = sol_1, m = 1.2),
sol_0.4 = simulate_policy(N0 = 800, n_years = 40, sol = sol_0.4, m = 1.2),
sol_1.25 = simulate_policy(N0 = 800, n_years = 40, sol = sol_1.25, m = 1.2)
)
ggplot() +
geom_line(aes(x = 1:40, y = traj_m1$sol_1$N), color = "cornflowerblue") +
#geom_line(aes(x = 1:40, y = traj_m1$sol_prob$N), color = "goldenrod") +
geom_line(aes(x = 1:40, y = traj_m1$sol_1.25$N)) +
geom_line(aes(x = 1:40, y = traj_m1$sol_0.4$N)) +
labs(x = "t", y = "N")
ggplot() +
geom_line(aes(x = 1:40, y = traj_m1$sol_1$reward), color = "cornflowerblue") +
#geom_line(aes(x = 1:40, y = traj_m1$sol_prob$reward), color = "goldenrod") +
geom_line(aes(x = 1:40, y = traj_m1$sol_1.25$reward)) +
geom_line(aes(x = 1:40, y = traj_m1$sol_0.4$reward)) +
labs(x = "t", y = "reward")
test <- as.data.frame(traj_m1$reward)
traj_m1_long <- data.frame(
t = c(1:40, 1:40, 1:40),
reward = c(traj_m1$sol_1$reward, traj_m1$sol_1.25$reward,
traj_m1$sol_0.4$reward),
m = c(rep(1, 40), rep(1.25, 40), rep(0.4, 40))
)
ggplot(data = traj_m1_long) +
geom_line(aes(x = t, y = reward, color = as.factor(m))) +
labs(x = "t", y = "reward", color = "m")
ggplot(data = traj_m1_long) +
geom_line(aes(x = t, y = reward, color = as.factor(m))) +
labs(x = "t", y = "reward", color = "m") +
ggtitle("True model: m = 1") +
theme(plot.title = element_text(color = "#00BA38"))
traj_m0.4_long <- data.frame(
t = c(1:40, 1:40, 1:40),
reward = c(traj_m0.4$sol_1$reward, traj_m0.4$sol_1.25$reward,
traj_m0.4$sol_0.4$reward),
m = c(rep(1, 40), rep(1.25, 40), rep(0.4, 40))
)
ggplot(data = traj_m0.4_long) +
geom_line(aes(x = t, y = reward, color = as.factor(m))) +
labs(x = "t", y = "reward", color = "m") +
ggtitle("True model: m = 0.4") +
theme(plot.title = element_text(color = "#F8766D"))
traj_m1.25_long <- data.frame(
t = c(1:40, 1:40, 1:40),
reward = c(traj_m1.25$sol_1$reward, traj_m1.25$sol_1.25$reward,
traj_m1.25$sol_0.4$reward),
m = c(rep(1, 40), rep(1.25, 40), rep(0.4, 40))
)
ggplot(data = traj_m1.25_long) +
geom_line(aes(x = t, y = reward, color = as.factor(m))) +
labs(x = "t", y = "reward", color = "m") +
ggtitle("True model: m = 0.4") +
theme(plot.title = element_text(color = "#619CFF"))
plot_1.25 <- ggplot(data = traj_m1.25_long) +
geom_line(aes(x = t, y = reward, color = as.factor(m))) +
labs(x = "t", y = "reward", color = "m") +
ggtitle("True model: m = 0.4") +
theme(plot.title = element_text(color = "#619CFF"))
plot_0.4 <- ggplot(data = traj_m0.4_long) +
geom_line(aes(x = t, y = reward, color = as.factor(m))) +
labs(x = "t", y = "reward", color = "m") +
ggtitle("True model: m = 0.4") +
theme(plot.title = element_text(color = "#F8766D"))
plot_1.25 <- ggplot(data = traj_m1.25_long) +
geom_line(aes(x = t, y = reward, color = as.factor(m))) +
labs(x = "t", y = "reward", color = "m") +
ggtitle("True model: m = 1.25") +
theme(plot.title = element_text(color = "#619CFF"))
plot_1 <- ggplot(data = traj_m1_long) +
geom_line(aes(x = t, y = reward, color = as.factor(m))) +
labs(x = "t", y = "reward", color = "m") +
ggtitle("True model: m = 1") +
theme(plot.title = element_text(color = "#00BA38"),
legend.position = "None")
plot_1
plot_0.4 <- ggplot(data = traj_m0.4_long) +
geom_line(aes(x = t, y = reward, color = as.factor(m))) +
labs(x = "t", y = "reward", color = "m") +
ggtitle("True model: m = 0.4") +
theme(plot.title = element_text(color = "#F8766D"),
legend.position = "None")
plot_1.25 <- ggplot(data = traj_m1.25_long) +
geom_line(aes(x = t, y = reward, color = as.factor(m))) +
labs(x = "t", y = "reward", color = "m") +
ggtitle("True model: m = 1.25") +
theme(plot.title = element_text(color = "#619CFF"))
library(patchwork)
install.packages("patchwork")
library(patchwork)
final_reward_plot <- plot_1 + plot_0.4 + plot_1.25 + plot_layout(nrow = 1)
final_reward_plot
library(tidyverse)
library(MDPtoolbox)
library(expm)
# remotes::install_github("boettiger-lab/mdplearning")
library(mdplearning)
install.packages("mdplearning")
states <- seq(0,24, length.out = 240)
actions <- states
obs <- states
reward_fn <- function(x,h) pmin(x,h)
discount <- 0.99
# K is at twice max of f3; 8 * K_3 / 5
f1 <- function(x, h = 0, r = 2, K = 10 * 8 / 5){
s <- pmax(x - h, 0)
s + s * (r * (1 - s / K) )
}
f2 <- function(x, h = 0, r = 0.5, K = 10){
s <- pmax(x - h, 0)
s + s * (r * (1 - s / K) )
}
# max is at 4 * K / 5
f3  <- function(x, h = 0, r = .002, K = 10){
s <- pmax(x - h, 0)
s + s ^ 4 * r * (1 - s / K)
}
## gather models together, indicate true model
sigma_g <- 0.05
models <- list("1" = f1, "2" = f2, "3" = f3)
model_sigmas <- c(sigma_g, 1.5 * sigma_g, sigma_g)
true_model <- "3"
transition_matrices <- function(f, states, actions, sigma_g){
n_s <- length(states)
n_a <- length(actions)
transition <- array(0, dim = c(n_s, n_s, n_a))
for (k in 1:n_s) {
for (i in 1:n_a) {
nextpop <- f(states[k], actions[i])
if(nextpop <= 0){
transition[k, , i] <- c(1, rep(0, n_s - 1))
} else if(sigma_g > 0){
x <- dlnorm(states, log(nextpop), sdlog = sigma_g)
if(sum(x) == 0){ ## nextpop is computationally zero
transition[k, , i] <- c(1, rep(0, n_s - 1))
} else {
x <- x / sum(x) # normalize evenly
transition[k, , i] <- x
}
}
}
}
transition
}
P <- lapply(seq_along(models), function(i)
transition_matrices(models[[i]], states, actions, model_sigmas[[i]]))
names(P) <- c("1", "2", "3")
## Compute reward matrix (shared across all models)
n_s <- length(states)
n_a <- length(actions)
reward <- array(0, dim = c(n_s, n_a))
for (k in 1:n_s) {
for (i in 1:n_a) {
reward[k, i] <- reward_fn(states[k], actions[i])
}
}
mdp <- memoise::memoise(mdp_value_iteration,
cache = memoise::cache_filesystem("cache/"))
policies <-
map_dfr(P,
function(P){
soln <- mdp(P, reward, discount = discount,
epsilon = 0.01, max_iter = 2000, V0 = rep(0,dim(P)[[1]]))
escapement <- states - actions[soln$policy]
tibble(states, policy = soln$policy, escapement)
},
.id = "model")
#write_csv(policies, "../data/policies.csv")
View(policies)
library(tidyverse)
library(mdptoolbox)
library(MDPtoolbox)
library(patchwork)
# Carrying capacity: upper bound defining the support for N
K <- 2000
# Discrete state space for abundance N:
#  - Here step = 20, so we have 101 states from 0 to 2000.
#  - Trade-off: finer step => more precise but slower.
seq_N <- seq(0, K, by = 20)
S <- length(seq_N)  # number of states
# Discrete action space for control effort u in [0,1]:
#  - Here step = 0.05, so we have 21 effort levels.
seq_u <- seq(0, 1, by = 0.05)
A <- length(seq_u)  # number of actions
# -----------------------------#
# 2) Parameters (toy / to calibrate)
# -----------------------------#
# Intrinsic growth rate (logistic dynamics): controls growth at low N
r <- 0.9
# Controls how quickly the removal fraction increases with effort u
# Larger alpha => smaller u already removes a lot (stronger response)
alpha <- 1.5
# Tolerable threshold (soft constraint): above this, penalties apply
N_tol <- 400
# -----------------------------#
# 3) Helper functions
# -----------------------------#
# Clamp a number into [lo, hi] to avoid leaving state support
clamp <- function(x, lo, hi) pmin(pmax(x, lo), hi)
# Project a continuous value onto a discrete grid by nearest neighbor
# (this is the "state discretization" step).
round_to_grid <- function(x, grid) {
grid[which.min((grid - x)^2)]
}
# Removal fraction as a saturating function of effort u:
#   q(u) = 1 - exp(-alpha*u)
# Properties:
#   q(0)=0, increasing, saturating (diminishing returns).
q_u <- function(u) 1 - exp(-alpha * u)
# -----------------------------#
# 4) Population dynamics (1-step)
# -----------------------------#
# Deterministic update:
#   N_{t+1} = N_t + r N_t (1 - N_t/K) - q(u) N_t
#
# Interpretation:
# - logistic growth adds recruits at low N and slows near K
# - control removes a fraction q(u) of the population
step_fun <- function(N, u, m) {
Nnext <- N + r * N * (1 - (N / K) ^ m) - q_u(u) * N
clamp(Nnext, 0, K)
}
# -----------------------------#
# 5) Reward function = negative total costs
# -----------------------------#
# Components:
# - damage(N): increasing convex function of N (impacts accelerate with abundance)
# - cost(u): increasing convex function of u (marginal effort becomes harder/costlier)
# - penalty(N): soft constraint if N > N_tol (risk aversion / unacceptable state)
reward_fun <- function(N, u) {
# Damages (toy): linear + quadratic
# - linear = baseline nuisance
# - quadratic = rapidly increasing impacts at high N
damage <- 0.1 * N + 0.001 * N^2
# Management cost (toy): convex in u
# - proportional part + quadratic part for increasing marginal costs
# cost <- 50 * u + 200 * u^2
cost <- 500 * u + 1000 * u^2
# cost <- 750 * u + 2000 * u^2
# Penalty above threshold (toy): increases linearly beyond N_tol
penalty <- if (N > N_tol) 2000 * (N - N_tol) / N_tol else 0
# Reward = negative costs (maximize reward <=> minimize cost)
-(damage + cost + penalty)
}
# get deterministic P
P <- list()
get_P <- function(m) {
P <- array(0, dim = c(S, S, A))
for (s in seq_len(S)) {
# Current abundance for state index s
N <- seq_N[s]
for (a in seq_len(A)) {
# Current effort for action index a
u <- seq_u[a]
# Continuous next abundance under dynamics + control
Nnext <- step_fun(N, u, m)
# Discretize to the nearest grid state
Ndisc <- round_to_grid(Nnext, seq_N)
# Convert discretized abundance value to a state index
s_next <- which(seq_N == Ndisc)
# Deterministic transition: prob 1 to s_next
P[s, s_next, a] <- 1
}
}
return(P)
}
m <- c(0.4, 1, 1.25)
m <- c(0.4, 1, 1.25)
for (i in seq_len(m)) {
print(m[i])
}
for (i in seq_len(length(m))) {
print(m[i])
}
m <- c(0.4, 1, 1.25)
for (i in seq_len(length(m))) {
P[[i]] <- get_P(m[i])
}
names(P) <- c("1", "2", "3")
# get reward
R <- matrix(0, nrow = S, ncol = A)
for (s in seq_len(S)) {
# Current abundance for state index s
N <- seq_N[s]
for (a in seq_len(A)) {
# Current effort for action index a
u <- seq_u[a]
# Immediate reward at (N,u)
R[s, a] <- reward_fun(N, u)
}
}
P[[1]]
dim(P[[1]])
P[[1]][, 1, ]
P[[1]][10, , ]
